# DroidNet Sentinel — Requirements
# pip install -r requirements.txt

# Core
# Rangos compatibles: permiten parches de seguridad sin saltos de mayor.
requests>=2.31,<3
rich>=13.7,<14
flask>=3.0,<4

# Opcional: solo si usas deauther.py en Linux/PC
# scapy>=2.5,<3

# Opcional: el dashboard ya trae rate-limit y CSRF in-memory propios. Si
# despliegas detrás de proxy reverso o con varios workers, sustitúyelos por:
# flask-limiter>=3.5,<4   # rate-limit con backend redis/memcached
# flask-wtf>=1.2,<2       # CSRF con tokens firmados
