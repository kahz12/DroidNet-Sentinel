# DroidNet Sentinel — Requirements
# pip install -r requirements.txt

# Core
# Compatible ranges: allow security patches without major version jumps.
requests>=2.31,<3
rich>=13.7,<14
flask>=3.0,<4

# Optional: only if using deauther.py on Linux/PC
# scapy>=2.5,<3

# Optional: the dashboard already includes its own in-memory rate-limiting and CSRF
# protection. If you deploy behind a reverse proxy or with multiple workers, 
# replace them with:
# flask-limiter>=3.5,<4   # rate-limiting with redis/memcached backend
# flask-wtf>=1.2,<2       # CSRF with signed tokens
